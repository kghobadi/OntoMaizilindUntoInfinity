using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Controls the behavior of a single spirit tendril until it hits the pilot. 
/// Either it disappears or is the first to activate the hallucination from the manager.
/// </summary>
public class SpiritTendril : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
